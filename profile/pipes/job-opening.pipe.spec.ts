import { JobOpeningPipe } from './job-opening.pipe';

describe('JobOpeningPipe', () => {
  it('create an instance', () => {
    const pipe = new JobOpeningPipe();
    expect(pipe).toBeTruthy();
  });
});
