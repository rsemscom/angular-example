import { TechnologiesFilterPipe } from './technologies-filter.pipe';

describe('TechnologiesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TechnologiesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
